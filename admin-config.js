const ADMIN_CONFIG = {
  password: 'evan@awards',

  // Local JSON file synced from Google Sheet (run sync-responses.ps1 to refresh)
  dataFile: 'data/responses.json',

  refreshSeconds: 60,
  topNominees: 10
};